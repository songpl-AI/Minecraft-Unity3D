# 体素模型生成器 🎮

> 基于JSON数据在Unity中生成3D体素模型，支持LLM集成

## ⚡ 30秒快速开始

1. 在Unity中创建空GameObject，添加 `VoxelModelTest` 组件
2. 运行游戏
3. 按 **G键** 生成狗模型

就这么简单！✨

## 📁 文件清单

### 核心脚本（必需）
- `Assets/Scripts/VoxelModelGenerator.cs` - 主生成器
- `Assets/Scripts/VoxelModelTest.cs` - 测试脚本

### 文档
- `快速上手指南.md` - **从这里开始** 👈
- `体素模型生成器使用说明.md` - 详细技术文档
- `体素模型生成系统_总结.md` - 系统架构说明
- `example_voxel_models.json` - 示例JSON数据

## 🎯 核心功能

```
自然语言描述 → LLM生成JSON → Unity生成3D模型
```

### 特点

✅ 使用现有方块系统（Grass、Dirt、Stone、Trunk、Leaves）  
✅ 高性能mesh合并（面剔除优化）  
✅ 自动地面检测和放置  
✅ 完整碰撞支持  
✅ 5分钟快速集成  

## 📝 JSON格式示例

```json
{
  "name": "SimpleDog",
  "size": {"x": 8, "y": 6, "z": 12},
  "voxels": [
    {"x": 2, "y": 0, "z": 2, "blockType": "Stone"},
    {"x": 3, "y": 1, "z": 3, "blockType": "Dirt"},
    ...
  ]
}
```

## 🎮 测试按键

运行游戏后：

- **G键** - 生成狗 🐕
- **H键** - 生成房子 🏠
- **T键** - 生成树 🌳
- **C键** - 清除所有模型 🧹

## 📚 使用方法

### 方法1：快速测试

```csharp
// 挂载VoxelModelTest脚本到GameObject
// 运行游戏，按G/H/T键测试
```

### 方法2：代码调用

```csharp
VoxelModelGenerator generator = gameObject.AddComponent<VoxelModelGenerator>();
generator.jsonData = yourJsonString;
generator.GenerateAndSpawnModel();
```

### 方法3：Inspector配置

1. 添加 `VoxelModelGenerator` 组件
2. 粘贴JSON到 "Json Data" 字段
3. 勾选 "Generate On Start"
4. 运行

## 🤖 与LLM集成

### 提示词示例

```
请生成一个体素模型JSON：
- 一只猫
- 使用方块：Grass, Dirt, Stone, Trunk, Leaves
- 大小：10x10x10以内
- JSON格式：{"name": "...", "size": {...}, "voxels": [...]}
```

### 获取JSON后

```csharp
generator.jsonData = llmResponse;
generator.GenerateAndSpawnModel();
```

## 📊 性能参考

| 模型尺寸 | 生成时间 | FPS影响 |
|---------|---------|---------|
| 5x5x5 | ~10ms | 无 |
| 10x10x10 | ~50ms | <1% |
| 20x20x20 | ~400ms | ~5% |

推荐尺寸：**10x10x10以内**

## 🔧 支持的方块类型

| 类型 | 说明 | 适用 |
|-----|------|-----|
| `Grass` | 草方块 | 地面 |
| `Dirt` | 泥土 | 身体 |
| `Stone` | 石头 | 基础 |
| `Trunk` | 树干 | 木材 |
| `Leaves` | 树叶 | 装饰 |

## 🐛 常见问题

**Q: 按G键没反应？**  
A: 检查是否挂载了 `VoxelModelTest` 组件，确认游戏正在运行

**Q: 模型看不见？**  
A: 可能在身后，尝试转身；或调整 `spawnDistance` 参数

**Q: JSON解析失败？**  
A: 使用在线JSON验证器检查格式，确认方块类型名称正确

**Q: 没有纹理？**  
A: 确保场景中有 `TerrainChunk` 对象（用于获取材质）

## 📖 详细文档

- **新手？** 阅读：`快速上手指南.md`
- **开发者？** 阅读：`体素模型生成器使用说明.md`
- **架构师？** 阅读：`体素模型生成系统_总结.md`

## 🎨 示例JSON

查看 `example_voxel_models.json` 获取更多示例：
- SimpleDog（狗）
- SimpleCube（立方体）
- SmallPyramid（金字塔）

## 🔮 应用场景

- 🎮 游戏内容程序化生成
- 🏗️ 关卡编辑器快速原型
- 🤖 AI驱动的3D建模
- 🎓 教学演示工具
- 🧪 快速测试体素算法

## 💡 实现原理

```
JSON数据
  ↓
解析为BlockType[,,]数组
  ↓
使用TerrainChunk的BuildMesh算法
  ↓
面剔除 + UV映射 + 三角形索引
  ↓
生成Mesh + MeshCollider
```

核心优势：完全复用现有代码，零额外依赖！

## 🚀 下一步

1. **测试基础功能**：运行游戏，按G键
2. **尝试修改JSON**：改变体素位置和类型
3. **集成到项目**：根据需要调用生成器
4. **连接LLM**：实现自然语言到3D模型

## 📞 需要帮助？

1. 查看 `快速上手指南.md` 的常见问题部分
2. 检查Unity Console的错误信息
3. 验证JSON格式和方块类型

## 📜 许可

本代码与项目主体使用相同的许可证。

---

**版本**：1.0  
**创建日期**：2026-01-12  
**兼容性**：Unity 2021.3+

开始你的体素建模之旅吧！🎉
